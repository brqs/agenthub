/**
 * AgentsPage — Agent management.
 *
 * TODO(F):
 *  - 列表（内置 / 自建分组）
 *  - 创建表单（CustomAgentForm）
 *  - 编辑 / 删除自建 Agent
 */

export function AgentsPage() {
  return (
    <div className="h-full overflow-y-auto p-6 scrollbar-thin">
      <h1 className="text-2xl font-bold mb-6">Agent 管理</h1>
      <div className="text-sm text-gray-400">TODO(F): Agent 列表 + 创建表单</div>
    </div>
  );
}
